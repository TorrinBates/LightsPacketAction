<UserControl x:Class="LightsPacketAction.ConfigureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightsPacketAction"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="#1D1D1D" BorderThickness="2" Margin="0,0,0,6">
            <Grid>
                <ScrollViewer>

                </ScrollViewer>
            </Grid>
        </Border>
        <WrapPanel Grid.Row="1" HorizontalAlignment="Center">
            <Button Style="{StaticResource MainButtonStyle}" Margin="25,0" HorizontalAlignment="Left" Width="80" Height="25" Command="{Binding CancelCommand}">Cancel</Button>
            <Button Style="{StaticResource MainButtonStyle}" Margin="25,0" HorizontalAlignment="Right" Width="80" Height="25">Apply</Button>
        </WrapPanel>
    </Grid>
</UserControl>
