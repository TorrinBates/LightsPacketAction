<UserControl x:Class="LightsPacketAction.LauncherView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightsPacketAction"
             mc:Ignorable="d" >

    <UserControl.Resources>
    <Style TargetType="TextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#c5c5c7"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#c5c5c7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    </UserControl.Resources>
    
    <StackPanel>
        <DockPanel Margin="0,0,10,10">
            <TextBlock FontSize="14" Foreground="#ED1C24" Text="Server Name/IP:"/>
            <TextBox DockPanel.Dock="Right" BorderBrush="#1D1D1D" Height="18" Width="300" Foreground="#ED1C24" Background="#3F3F46"></TextBox>
        </DockPanel>
        <DockPanel Margin="0,0,10,10">
            <TextBlock FontSize="14" Foreground="#ED1C24" Text="Port:"/>
            <TextBox DockPanel.Dock="Right" BorderBrush="#1D1D1D" Height="18" Width="300" Foreground="#ED1C24" Background="#3F3F46"></TextBox>
        </DockPanel>
        <DockPanel Margin="0,0,10,10">
            <TextBlock FontSize="14" Foreground="#ED1C24" Text="Overlay:"/>
            <TextBox DockPanel.Dock="Right" BorderBrush="#1D1D1D" Height="18" Width="300" Foreground="#ED1C24" Background="#3F3F46"></TextBox>
        </DockPanel>
    </StackPanel>
</UserControl>
