<UserControl x:Class="LightsPacketAction.DualButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightsPacketAction"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <WrapPanel HorizontalAlignment="Center">
            <Button Style="{StaticResource MainButtonStyle}" Margin="25,0" HorizontalAlignment="Left" Width="80" Height="25" Command="{Binding PreviousCommand}">Back</Button>
            <Button Style="{StaticResource MainButtonStyle}" Margin="25,0" HorizontalAlignment="Right" Height="25" Width="80" Command="{Binding ApplyCommand}">Apply</Button>
        </WrapPanel>

    </Grid>
</UserControl>
